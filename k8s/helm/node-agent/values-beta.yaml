# Beta environment values for node-agent
# Shared testing environment (dal2.penguintech.cloud)

namespace: tobogganing

image:
  repository: ghcr.io/penguintechinc/tobogganing/node-agent
  tag: beta-1724152800
  pullPolicy: IfNotPresent

nodeAgent:
  mode: daemonset
  controlPlaneUrl: "https://hub-api.tobogganing.svc:8443"
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi
