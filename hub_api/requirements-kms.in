# Optional KMS provider dependencies for Enterprise external KMS support.
# Install with: uv pip install --require-hashes -r core/requirements-kms.txt
# These are NOT included in the base requirements.txt (Community/default in-app only).

boto3==1.43.48                    # AWS SDK
google-cloud-kms==3.15.0          # Google Cloud KMS client
cryptography>=50.0.0              # transitive via boto3/google-cloud-kms; pinned to clear PYSEC-2026-3552
