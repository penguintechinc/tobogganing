apiVersion: v1
kind: Namespace
metadata:
  name: tobogganing
  labels:
    app.kubernetes.io/name: tobogganing
    app.kubernetes.io/component: namespace
---
# Service Account for Tobogganing components
apiVersion: v1
kind: ServiceAccount
metadata:
  name: tobogganing
  namespace: tobogganing
  labels:
    app.kubernetes.io/name: tobogganing
    app.kubernetes.io/component: serviceaccount
---
# Network Policy for Tobogganing namespace
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: tobogganing-network-policy
  namespace: tobogganing
  labels:
    app.kubernetes.io/name: tobogganing
    app.kubernetes.io/component: networkpolicy
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: tobogganing
    # Allow ingress from ingress controllers
  - from: []
    ports:
    - protocol: TCP
      port: 8000  # Manager API
    - protocol: TCP
      port: 8080  # Headend proxy
    - protocol: UDP
      port: 51820 # WireGuard
  egress:
  - {} # Allow all egress traffic