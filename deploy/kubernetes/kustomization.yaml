# Kustomization file for Tobogganing deployment
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: tobogganing
  annotations:
    config.kubernetes.io/local-config: "true"

# Namespace for all resources
namespace: tobogganing

# Common labels applied to all resources
commonLabels:
  app.kubernetes.io/name: tobogganing
  app.kubernetes.io/instance: production
  app.kubernetes.io/part-of: tobogganing-platform
  app.kubernetes.io/managed-by: kustomize

# Resources to include
resources:
- namespace.yaml
- configmap.yaml
- secrets.yaml
- redis.yaml
- manager.yaml
- headend.yaml
- ingress.yaml
- hpa.yaml
- monitoring.yaml

# Image transformations
images:
- name: ghcr.io/your-org/tobogganing/hub-api
  newTag: latest
- name: ghcr.io/your-org/tobogganing/hub-router
  newTag: latest

# Patches for environment-specific customizations
patchesStrategicMerge:
# Uncomment and modify for environment-specific overrides
# - patches/production.yaml

# ConfigMap and Secret generators
configMapGenerator:
- name: tobogganing-env-config
  literals:
  - ENVIRONMENT=production
  - DEPLOYMENT_TYPE=kubernetes
  - MONITORING_ENABLED=true

# Replica overrides for production
replicas:
- name: hub-api
  count: 3
- name: redis
  count: 1  # Redis should remain single instance unless using clustering

# Resource patches
patchesJson6902:
- target:
    group: apps
    version: v1
    kind: Deployment
    name: hub-api
  patch: |-
    - op: replace
      path: /spec/template/spec/containers/0/resources/requests/memory
      value: "512Mi"
    - op: replace
      path: /spec/template/spec/containers/0/resources/limits/memory
      value: "1Gi"

- target:
    group: apps
    version: v1
    kind: Deployment
    name: hub-router
  patch: |-
    - op: replace
      path: /spec/template/spec/containers/0/resources/requests/memory
      value: "512Mi"
    - op: replace
      path: /spec/template/spec/containers/0/resources/limits/memory
      value: "2Gi"